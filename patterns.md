# 工程模式目录

> 从子项目开发中提炼的、可跨项目复用的工程模式。
> 每个模式包含：问题 → 方案 → 实例（teacher 项目中的具体体现）→ 适用条件。

## 1. 三层版本化架构

**问题**：项目既有通用规则（跨实例复用），又有执行逻辑（如何操作），又有实例数据（具体内容），三者混在一起导致"改规则时不知道要改多少地方"。

**方案**：
- 规则源（core/）：宪法级，定义"是什么"和"为什么"，独立于任何具体实例
- 执行器（skill/）：定义"怎么做"，引用规则源，随规则源升级而同步升级
- 实例层（proj-N/）：存储数据，不包含规则逻辑
- 每层独立版本号，层级间有明确的升级传导规则

**teacher 实例**：`philosophy.md + qb-spec.md`（规则源 v0.3.0）→ `teacher.md`（skill v0.2.0，P3 v0.2.0，P4 v0.3.0）→ `proj-1/`（实例数据）

**适用条件**：项目存在"通用框架 + 多个实例"的结构，且框架本身需要迭代。

---

## 2. 流程约束优于声明约定

**问题**：在文档中写了"应该做 X"，但执行时依赖记忆，导致遗漏。用户不得不事后检查。

**方案**：如果一个动作是"必须做"的，把它变成流程中的**命名步骤**（Step N），而非靠 `约定` 章节中的声明性文字。步骤有明确的输入/输出/完成条件。

**teacher 实例**：
- P4 原来只有"写入 learnings"，`升级约定` 中说"规则源升级→skill 同步"——但这是声明，不是步骤 → 被遗忘
- 修复：P4 拆为 Step 1（反思写入）+ Step 2（传导检查，强制），P4 完成 = 三项全部完成
- 同理：P3 的漂移检测变成 Step 0，不是脚注

**适用条件**：任何需要跨会话可靠执行的流程。宣言式的 rule 靠人记忆，管道式的 step 靠流程保证。

---

## 3. 设计日志机制

**问题**：项目开发过程中，用户会提出关于架构、流程、设计原则的讨论和要求。这些讨论本身就是项目演进的历史，但如果不记录，就会被后续对话淹没。

**方案**：
- 建立 `design-log.md`，按日期记录所有项目级讨论
- 格式：原文引用 + 结构化要点 + 影响追踪
- 取舍标准：涉及架构/算法/流程/原则 → 必记；纯操作 → 视情况
- 记录时机：当次讨论结束后立即写入，不回填历史

**teacher 实例**：`design-log.md` 已记录 3 条讨论（漂移问题、传导缺失、设计日志需求本身）

**适用条件**：任何需要持续迭代的项目，且项目设计决策需要可追溯。

---

## 4. 漂移检测与回归引力

**问题**：探索系统根据算法推荐方向，但算法可能形成正反馈（新线索得分高→用户选新线索→新线索更深→得分更高），导致原始目标被系统性忽略。

**方案**：
- 在工作记忆中追踪 `drift_count`（连续在非原始锚点探索的轮数）
- 算法增加 `回归引力 (recall)` 因子：漂移超过阈值时自动为原始目标加权
- 新增线索的分数随深度衰减（L1=1, L2=0.6, L3=0.3）
- 每次打开新增线索时，显式提示用户"这是分叉，是否继续？"

**teacher 实例**：proj-1 从宣传文化工作漂移到党委治理，连续 3 轮后触发漂移检测。修复后 v0.3.0 的 P3 包含 Step 0 漂移检测。

**适用条件**：任何有外部框架/提纲约束的探索任务。

---

## 5. 双轨开发

**问题**：开发一个工具，需要真实场景来测试。同时，真实场景本身也是一个需要完成的任务。

**方案**：工具开发（teacher）和领域任务（proj-1）同步进行，领域任务是工具的测试载体。领域中发现的问题 → 改进工具；工具改进 → 更好地服务领域。

**teacher 实例**：proj-1 的"公募基金宣传文化工作调研"既是真实的调研任务，也是 teacher 方法论的测试床。漂移问题正是在真实探索中暴露的，而非凭空设计。

**适用条件**：开发方法论/框架/工具型项目时，需要同步运行一个领域实例作为验证。

---

## 6. 用户反馈即时结构化

**问题**：用户指出一个问题时，如果只是"修好这个问题"，同样的模式会在其他环节重现。因为问题根因是结构性的，不是局部的。

**方案**：
- 收到用户反馈 → 不满足于修局部 → 追问"这个问题的根因是什么？在其他地方会不会重现？"
- 如果根因是流程/结构缺陷 → 修改流程/结构本身，让同类问题不再可能发生
- 如果反馈本身揭示了新的需求类别 → 建立新的机制来承载这类需求

**teacher 实例**：
- 用户："为什么没有同步更新 skill？" → 不是补上这次更新就完了 → 根因是 P4 缺少强制传导步骤 → 修改 P4 结构 → 同类问题不再可能
- 用户："项目讨论需要记录" → 不是手动记几条 → 建立 design-log.md 作为持久机制

**适用条件**：任何需要持续迭代的人机协同项目。用户的每一次纠偏都是流程改进的机会。

---

## 7. 可复用操作封装为 Skill

**问题**：一个涉及多文件创建/修改的初始化操作，每次需要手动执行相同步骤——创建目录、复制模板、替换占位符、更新注册表。不仅繁琐，而且容易遗漏步骤。

**方案**：
- 将操作封装为独立 skill（如 `proj-init`），包含管道步骤和内置模板资源（assets/）
- 模板使用占位符（`{N}`, `{topic}`, `{date}`），skill 执行时自动替换
- skill 自包含（不依赖外部模板目录），安装即用
- 通过 slash command 触发，降低使用门槛

**teacher 实例**：`proj-init` skill 封装了新建 proj-N 的全流程。模板文件放在 `assets/`，CLAUDE.md 注册表更新逻辑写入 SKILL.md 管道步骤。用户只需 `/proj-init` + 提供主题即可。

**适用条件**：任何需要重复执行的、涉及多文件操作的项目初始化或搭建任务。

---

## 8. 对话输出文档化

**问题**：在对话式 AI 交互中，AI 生成的重要产出（教学内容、分析结论、设计决策）散落在聊天记录中。用户事后难以搜索、引用或回顾，每次需要时都要翻几十轮对话。

**方案**：
- 在完成一个逻辑单元的工作后，自动将对话中的关键产出整理为结构化文档
- 文档保存到指定文件位置（而非留在聊天中）
- 输出时明确告知用户文档路径，方便后续调阅
- 文档是"板书笔记"——精简、结构化、可搜索，不同于原始对话的"教学录音"

**mentor 实例**：P3 教学文档输出——每完成一个单元，自动生成 `proj-N/teaching-docs/[单元名].md`，包含概念速查表、类比、图示、练习。用户可随时打开文件回顾，无需翻聊天记录。

**适用条件**：任何对话式 AI 系统，其产出需要被用户后续回顾和引用——教学、代码审查报告、调研报告、设计讨论记录等。尤其适用于产出量大、跨多轮对话的长期任务。
