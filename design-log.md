# 元项目设计日志

> 记录元项目自身的观点、讨论、需求和决策。

## 2026-05-28 — 元项目建立

### 来源
teacher 项目开发会话。用户在 teacher v0.3.0 开发过程中提出。

### 原文
> "我需要每个项目设置一个上级接口，所谓上级就是'元项目'，元项目涉及如何充分开发项目，如何促进人机合作，如何改进开发流程，如何充分发挥AI能力等更加底层的问题，这些经验的积累可以在后续迁移到之后的项目开发中。"

### 要点
- 元项目定位为"上级项目"，各子项目通过 `上级接口` 指向
- 核心命题 4 项：人机协同 / 开发流程 / AI 能力发挥 / 工程模式
- 触发：子项目 P4 Step 2-B 的传导判定（3 条件逐条检查）
- 元项目自身也需要设计日志（即本文件），记录元项目级的决策

---

## 2026-05-28 — 元项目沉淀触发规则

### 来源
teacher 项目开发会话。用户追问触发机制。

### 原文
> "元经验沉淀目前的触发规则是什么，我后续不会再强调这个问题了，但是我希望在这个项目结束之后，元项目已经积累得很丰富了"

### 要点
- 触发规则已嵌入 teacher skill P4 Step 2-B，作为管道强制步骤
- 判定标准（3 条件）：非领域特定 / 可表达为模式 / 经实践验证
- 元项目设计日志应记录：每次沉淀了什么、为什么、效果如何

---

## 2026-05-28 — 创建 proj-init skill：多实例模板化初始化

### 来源
teacher 项目，proj-2 方案细化后的落地步骤。

### 原文
> "现在在元项目中创建一个skill，实现上述方案的可复用执行，做到，后续我只要调用该skill，就能直接开辟新proj，完成搭建所有必要文件和目录等准备工作。"

### 要点
- 将"新建 proj-N"从手动操作封装为可调用 skill，实现 5 个模板文件复制 + CLAUDE.md 注册表更新
- skill 采用内置模板（assets/）而非依赖外部 teacher/proj-template/，保证自包含
- 模板包含：question-bank.md（空注册表）、session-context.md（空工作记忆）、learnings.md、current-questions.md（空视图）、调研提纲.md（格式说明）
- 自动检测 N 值、替换占位符、维护 CLAUDE.md 活跃实例注册表
- 这是模式 2（流程约束优于声明约定）的又一次体现——"新建实例"从口头方案变成可执行管道

---

## 2026-05-31 — 新建 mentor 子项目：教授型学习系统

### 来源
mentor 项目初始化会话。用户在 teacher 之后提出开发互补的"纯教授型"学习系统。

### 原文
> "我要开发一个教程项目，不同于teacher项目是引导型，这个项目是纯教授型，我在任意一个领域的小白，他会给我规划最短学习路径，遵循第一性原理、金字塔原理等等各种科学的认知原理"

### 要点
- mentor 与 teacher 形成互补：teacher 是引导型（用户驱动探索），mentor 是教授型（AI 驱动教学）
- mentor 的核心能力：领域解构（第一性原理）→ 路径生成（帕累托+ZPD+拓扑排序）→ 内容教授（费曼+布鲁姆+精加工）→ 评估巩固（间隔重复+刻意练习）
- 遵循 12 条认知科学原理，分 4 层对应 5 Phase 管道
- 架构复用元项目模式 1（三层版本化）+ 模式 2（流程约束）+ 模式 3（设计日志）
- proj-N 多实例模式与 teacher 一致，后续可复用 proj-init skill

### 影响
- 创建 `mentor/` 项目（CLAUDE.md + core/ 4 规则源 + skill + proj-template 5 模板）
- meta-project CLAUDE.md 子项目注册表新增 mentor 条目

---

## 2026-05-31 — mentor 首次实战反馈：流程约束再次验证 + 新模式提炼

### 来源
mentor proj-1（GitHub 基础使用）P3 实战。用户反馈教学推进太快、缺少交互。mentor P4 Step 5 传导至元项目。

### 原文
> "你现在推进的速度太快了，都没有给我交互的余地，我得一步步交互才知道啊，而且在我们经过对话交互之后，你给我形成一个mentor文档更合适，我可以反复调阅"

### 要点
- **模式 2（流程约束优于声明约定）跨项目验证**：teacher 的 P4 传导缺失 → mentor 的 P3 交互缺失，同根因——"声明性约定靠不住"不是 teacher 特有问题，是人机协同的通用问题
- **提炼模式 8（对话输出文档化）**：从"教学后需要可调阅文档"中抽象——任何对话式 AI 的重要产出都应自动生成结构化文档
- mentor skill v0.2.0 修复：费曼验证 🔒 不可跳过 + 单次会话上限 + 教学文档输出

### 影响
- `patterns.md` → 新增模式 8「对话输出文档化」
- `learnings.md` → 追加模式 2 跨项目验证记录、模式 8 提炼记录
