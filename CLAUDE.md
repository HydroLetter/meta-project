# 元项目 (meta-project)

> 上级项目。积累"如何开发项目"的方法论——人机协同、工程流程、AI 能力发挥等底层经验。
> 跨项目复用，不绑定任何具体领域。

## 定位

- **元项目回答的问题**：怎样才能把项目开发好？人机如何高效协同？什么样的流程和机制是有效的？
- **与子项目的关系**：每个具体项目（如 teacher）通过 `上级接口` 字段指向本目录。开发子项目过程中涌现的项目管理方法论，沉淀到元项目中。
- **子项目不重复造轮子**：元项目中已有的模式，子项目直接引用，不在本地重复定义。

## 文件结构

```
meta-project/
  CLAUDE.md         # 本文件
  learnings.md       # 元经验积累（按次追加）
  patterns.md        # 可复用的工程模式（提炼后的知识晶体）
```

## 沉淀标准

- **来源**：任何子项目开发过程中暴露的流程问题、协同摩擦、机制改进需求
- **触发机制**：子项目 P4 Step 2 传导检查中，逐条判定每条可迁移洞察/方法调优是否满足三个条件：
  1. **非领域特定** — 不绑定子项目领域
  2. **可表达为模式** — 能用"问题→方案→适用条件"结构描述
  3. **经实践验证** — 在子项目中至少出现过 1 次真实案例
- **满足 3 条** → 写入 `patterns.md`（新模式）或 `learnings.md`（新经验）
- **满足部分** → 子项目 `learnings.md` 中标注"待观察"
- **均不满足** → 跳过
- **排除**：单个子项目特有的领域问题（如 teacher 的"探索漂移"是领域问题，但"流程约束优于声明约定"是元问题）
- **注意**：触发机制已嵌入子项目 skill 的 P4 Step 2，作为管道的强制步骤执行，不依赖人工记忆

## 子项目注册

| 项目 | 路径 | 状态 |
|------|------|------|
| teacher | ../teacher/ | 开发中 v0.3.0 |
| mentor | ../mentor/ | 开发中 v0.1.0 |
